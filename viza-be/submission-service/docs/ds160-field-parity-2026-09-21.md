# B1/B2 DS-160 核验与修复记录（2026-09-21）

本轮范围为当前 B1/B2。字段和分支已有逐项对照记录，发现的实现差异已修复发布；
这不等于已证明所有答案组合、服务器规则和最终提交路径完全一致。
`officialParityVerified` 保持 `false`。

## 核验范围和证据分类

| 项目 | 结果 | 边界 |
| --- | ---: | --- |
| 模型字段记录 | 337/337 有对照记录 | 331 项关联官网页面控件组；6 项为兼容字段，官网不独立显示 |
| schema 条件记录 | 83/83 有正反侧记录 | 包含兼容条件；不代表每个答案组合都独立提交过 |
| 实际重复组 | 21/21 有增删观察记录 | 所见行数不推断为官网最大行数 |
| 单项说明框 | 2 | 拒签和移民申请解释没有 Add/Remove，按结构不适用记录 |
| 官网原始记录 | 832 控件、365 页面状态 | 31 组选项、211 个主国籍门控结果、27 个安全问题标签 |
| 审计证据槽位 | 552 | 545 项现场观察，4 项增删结构不适用，3 项范围缺口 |

上述计数是当前内部模型的证据索引，不是官网字段数量的独立证明。条件计数来自
schema 的 showIf；年龄、国籍、婚姻和停留单位等额外门控另有现场记录。
安全问题保存了逐个 Yes 状态及五页 all-No 状态，不能把页面控件观察推广成所有
服务器校验都已通过。

可追溯资料：

- [原始浏览器记录](./ds160-live-browser-raw-evidence-2026-09-21.json)：无答案值、账号或申请人资料；保留控件 ID、属性、时间、页面状态和动作。
- [页面证据索引](./ds160-live-evidence-index-2026-09-21.json)：24 组 parentFindingId，覆盖全部 365 个原始状态。covers 表示与控件组的关联，不伪装成逐字段独立服务器测试。
- [当前字段与分支清单](./ds160-current-live-evidence-manifest-2026-09-21.json)：570 条证据记录均附 parentFindingIds，区分现场观察、契约比较和结构不适用。
- [观察日志及后续补录](./ds160-live-audit-2026-09-21.json)：保留早期观察及后续来源；较早的未观察说明不覆盖后来已有原始记录的控件状态。
- [空白证据模板](./ds160-field-parity-evidence-template.json)：没有现场证据，默认不能通过；与已填清单分开保存。

原始记录 SHA-256：
`6B96F82A14598A48E5DE208C425408183E6D34F8A93B4B74427EFA8E8758BBE4`。
控件 required 属性仅表示 HTML 属性；CEAC 的服务器校验可能另行执行，不能将
required=false 当作选填证明。整组证据和兼容字段均已显式分类。

## 已修复并验证

- 使用 CEAC 的国家、国籍、护照签发地、州和领馆选项及官方代码；中文填写界面保留中文展示。
- 对齐日期精度、字符长度、明确“不适用/不知道”、国籍重复、婚姻、职业、短时停留和 ESTA 等规则。
- 隐藏仅用于历史兼容的旧社交媒体、手机号、工作头衔等问题，避免干扰当前必填和审核。
- 跨步骤条件使用当前草稿更新；修复 A→B→A 回到已保存答案时未刷新分支的问题，同时保持普通输入的组件引用稳定。
- 数据库 337 项字段元数据与 seed 回读一致，非文案验证规则和有效选项代码无差异；未改写申请人答案。

内部 seed→runner→fixture 审计通过，缺失 runner 输入、fixture 输入、未消费字段均为 0。
前端相关回归、类型检查及 lint 通过；提交服务 DS-160/CEAC 回归和类型检查通过；
证据工具另有 7 项回归测试，防止把 DOM 观察标成逐项 officialVerified、把单项说明框
统计为增删操作，以及漏算范围缺口。

生产模式本地浏览器使用真实 React 页面及内存 mock actions 验证了：空必填不入队、
完整路径只入队一次、成功后只读和 PDF 入口、国籍 Japan→China→Japan、小时→天、
重复国籍、未知父母生日、中文州选项。该模拟提交不等于新的官方递交。

## 发布结果

产品代码 commit：`7e3b9cde326c1ead163ef5f76faab1ee999ce78e`。
前端 deployment：`dpl_BCPJa61WVGSYz1JnRTvL3nQXKbxj`，READY，已绑定
[app.viza.it.com](https://app.viza.it.com)。刷新真实已提交申请后页面正常、无客户端异常，
只读状态和确认页下载入口保留。本轮没有重复递交真实申请。

提交镜像：`registry.fly.io/viza-prod-runner-pool:ds160-7e3b9cde`，digest
`sha256:a18bcdc5d0b6ebbfafee12dafaaeaf436589bdfb88bf4174d7a17133e4ee5879`。
10 台 pool 和 1 台 legacy 均已更新并保持 stopped；排除镜像及 Fly 平台 metadata 后
配置变化为 0，预约专属环境和机器资源保留。远程 builder 已 suspended/stopped 且非计费。
没有启动或重放旧任务。后续证据分类、文档和测试更新不改变上述产品运行代码。

## 尚未证明的范围

1. 本轮独立 QA 的合格照片上传成功路径。
2. 本轮独立 QA 的最终 e-Sign、服务器签名校验和最终递交；合成资料没有签名提交。
3. 全部答案组合的回发、刷新恢复、旧条件值清理及服务器校验。代表性浏览器回归已通过，不能外推为穷尽验证。

此前真实申请的官方确认仅证明那次申请成功，不能替代本轮所有分支的验证。
因此，本轮可确认“字段与分支有可追溯对照，已修复的实现通过相关回归并上线”，
不能确认“官网所有行为 100% 一模一样”。

## 复现

在 submission-service 目录运行：

```text
node scripts/index-ds160-live-browser-evidence.mjs
npx tsx scripts/audit-ds160-field-parity.ts --evidence docs/ds160-current-live-evidence-manifest-2026-09-21.json --json
```

索引只引用已保存的官网原始记录，不访问或修改真实申请。
